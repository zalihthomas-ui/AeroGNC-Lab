[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "aerognc-lab"
version = "0.8.0"
description = "A modular flight dynamics, guidance, navigation and control verification platform"
readme = "README.md"
requires-python = ">=3.12"
license = {file = "LICENSE"}
authors = [{name = "Zalih Thomas"}]
keywords = ["aerospace", "flight-dynamics", "gnc", "simulation", "verification"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Science/Research",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Scientific/Engineering",
  "Typing :: Typed",
]
dependencies = [
  "matplotlib>=3.9,<4",
  "numpy>=2.0,<3",
  "Pillow>=10,<13",
  "PyYAML>=6.0,<7",
  "scipy>=1.14,<2",
]

[project.optional-dependencies]
dev = [
  "mypy>=1.11,<3",
  "pytest>=9.0.3,<10",
  "pytest-cov>=5,<7",
  "ruff>=0.6,<1",
  "types-PyYAML>=6.0,<7",
]

[project.scripts]
aerognc = "aerognc.cli:main"

[project.urls]
Homepage = "https://github.com/zalihthomas-ui/AeroGNC-Lab"
Documentation = "https://github.com/zalihthomas-ui/AeroGNC-Lab/tree/main/docs"
Repository = "https://github.com/zalihthomas-ui/AeroGNC-Lab.git"
Issues = "https://github.com/zalihthomas-ui/AeroGNC-Lab/issues"
Changelog = "https://github.com/zalihthomas-ui/AeroGNC-Lab/blob/main/CHANGELOG.md"

[tool.hatch.build.targets.wheel]
packages = ["src/aerognc"]

[tool.pytest.ini_options]
addopts = "--strict-config --strict-markers --import-mode=importlib -ra"
testpaths = ["tests"]
markers = [
  "validation: independent or analytical validation tests",
  "slow: tests that run a simulation ensemble",
]

[tool.coverage.run]
branch = true
source = ["aerognc"]
# Tk event-loop wiring is exercised by a live-window smoke check; the mission
# planning, propagation, uncertainty, and plotting services behind it remain in
# automated branch coverage.
omit = [
  "*/aerognc/visualisation/mission_designer.py",
  "*/aerognc/visualisation/workbench.py",
  # Tk event-loop view; its pure PlannerModel logic is covered by unit tests and
  # the window is exercised by a widget-construction smoke check.
  "*/aerognc/visualisation/mission_planner_map.py",
]

[tool.coverage.report]
show_missing = true
skip_covered = true
fail_under = 75
precision = 2

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "SIM", "RUF"]
ignore = ["N803", "N806"]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["N802"]
"src/aerognc/visualisation/mission_designer.py" = ["E501", "RUF001"]
"src/aerognc/project/report.py" = ["E501"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_unreachable = true
packages = ["aerognc"]
