# Examples

After editable installation, run `python examples/run_nominal.py` for the smallest
programmatic example. Full reproducible workflows are exposed through
`python -m aerognc.cli --help`; transient outputs are written under `results/`.
